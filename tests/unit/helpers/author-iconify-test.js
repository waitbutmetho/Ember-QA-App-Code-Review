import { authorIconify } from 'js-ember-code-review/helpers/author-iconify';
import { module, test } from 'qunit';

module('Unit | Helper | author iconify');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = authorIconify([42]);
  assert.ok(result);
});
